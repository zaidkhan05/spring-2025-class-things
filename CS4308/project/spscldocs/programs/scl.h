/* Systems Computing Language (SCL) */
#include <stdio.h> 
#include <stdlib.h> 
#include <string.h>     
#include <math.h>   
 
// #include "basic_lib.h"

enum Boolean {false, true};
typedef enum Boolean bool;
typedef unsigned char byte;
